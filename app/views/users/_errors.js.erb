// File needs to be deleted in the end if not used
$("ul.error_login").html("")
<% if @errors.any? %>
  <% @errors.each do |error| %>
    $("ul.error_login").append($("<li />").html("Fail"))
  <% end %>
<% end %>
